# Minimal manifest for CI and local smoke tests (no large clones required for validation-only checks).
repositories:
  - name: tiny-example
    language: python
    git: https://github.com/octocat/Hello-World.git
    commit: master
    adapters: [ruff]
